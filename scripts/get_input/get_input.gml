function get_input(){
	
	right = keyboard_check(vk_right);
	left = keyboard_check(vk_left);
	attack_button = mouse_check_button_pressed(mb_left);
	roll = keyboard_check(vk_alt);
	jump = keyboard_check(vk_space);
	
}